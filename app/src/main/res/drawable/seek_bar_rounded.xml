<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape
            android:shape="line">
            <corners android:radius="10dp"/>
            <stroke
                android:color="@color/seekbar_gray"
                android:width="5dp"/>
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape
                android:shape="line">
                <corners android:radius="10dp"/>
                <stroke
                    android:color="@color/seekbar_gray"
                    android:width="4dp"/>
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape
                android:shape="line">
                <corners android:radius="10dp"/>
                <stroke
                    android:color="@color/seekbar_gray"
                    android:width="4dp"/>
            </shape>
        </clip>
    </item>
</layer-list>